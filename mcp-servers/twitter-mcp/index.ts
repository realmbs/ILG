/**
 * ILG Twitter/X MCP Server
 *
 * Status: Phase 2 stub
 *
 * Tools to implement:
 * - search_recent(query, max_results)       — Search recent tweets by keyword/hashtag
 * - get_user_tweets(username, max_results)   — Get recent tweets from a specific user
 * - search_with_context(query, max_results)  — Search with full tweet context (author, metrics)
 *
 * Auth: Bearer token (apply at https://developer.x.com/en/portal/dashboard)
 * Rate limit: 1,500 reads/month total (HARD STOP at 1,400)
 *
 * CRITICAL: Every tool call must log to api_usage table.
 * This is the tightest rate limit in the entire system.
 */

console.log("Twitter MCP server — not yet implemented (Phase 2)");
console.log("Apply for API access at https://developer.x.com/en/portal/dashboard");
console.log("Add bearer token to mcp-servers/twitter-mcp/.env");
